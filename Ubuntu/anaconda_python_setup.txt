
 conda update --prefix C:\Users\kgoginen\AppData\Local\Continuum\anaconda3 anaconda
 
 sudo apt-get install libgl1-mesa-glx libegl1-mesa libxrandr2 libxrandr2 libxss1 libxcursor1 libxcomposite1 libasound2 libxi6 libxtst6
 
 
 bash Miniconda3-latest-Linux-x86_64.sh
 
 
 conda config --set auto_activate_base false
 
#########################################
 
Updating Anaconda after installing Miniconda as a User in order below:
 
conda update conda

conda install cython
conda install pylint
conda install autopep8 
conda install paramiko
conda install pyserial
conda install conda-forge::pyvisa
conda install conda-forge::pyvisa-py

conda install anaconda
conda install anaconda-navigator
conda install tensorflow

conda update --all
conda clean --all

##############################################
After installing Anaconda as User, in the base environment install:

conda install anaconda-navigator numpy pandas matplotlib seaborn scikit-learn jupyter notebook spyder ipython cython pylint autopep8 paramiko pyserial tensorflow keras pytorch

conda create -n radioconda -c conda-forge -c ryanvolz --only-deps radioconda
##############################################
 Uninstalling Anaconda:
 
sudo rm -rf ~/anaconda3
sudo rm -rf ~/miniconda3


Remove in ~/.bashrc:


cat ~/.bashrc

sudo nano ~/.bashrc


# added by Anaconda3 installer
export PATH="/home/linuxize/anaconda3/bin:$PATH"


# >>> conda initialize >>>
# !! Contents within this block are managed by 'conda init' !!
__conda_setup="$('/home/ocn-rd-rpower/miniconda3/bin/conda' 'shell.bash' 'hook' 2> /dev/null)"
if [ $? -eq 0 ]; then
    eval "$__conda_setup"
else
    if [ -f "/home/ocn-rd-rpower/miniconda3/etc/profile.d/conda.sh" ]; then
        . "/home/ocn-rd-rpower/miniconda3/etc/profile.d/conda.sh"
    else
        export PATH="/home/ocn-rd-rpower/miniconda3/bin:$PATH"
    fi
fi
unset __conda_setup
# <<< conda initialize <<<












rm -rf ~/.condarc ~/.conda ~/.continuum
